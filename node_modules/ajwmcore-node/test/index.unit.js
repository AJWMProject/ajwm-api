'use strict';

var should = require('chai').should();

describe('Index Exports', function() {
  it('will export ajwmcore-lib', function() {
    var ajwmcore = require('../');
    should.exist(ajwmcore.lib);
    should.exist(ajwmcore.lib.Transaction);
    should.exist(ajwmcore.lib.Block);
  });
});
